increment_weekly_counter:
  fields:
    child_id:
      required: true
      example: kid_1
      selector:
        text:

    amount:
      required: false
      default: 1
      example: 1
      selector:
        number:
          min: 1
          max: 100
          step: 1
          mode: box

    reason:
      required: false
      example: Manual reward adjustment
      selector:
        text:

decrement_weekly_counter:
  fields:
    child_id:
      required: true
      example: kid_1
      selector:
        text:

    amount:
      required: false
      default: 1
      example: 1
      selector:
        number:
          min: 1
          max: 100
          step: 1
          mode: box

    reason:
      required: false
      example: Manual correction
      selector:
        text:

add_child:
  fields:
    name:
      required: true
      example: Alex
      selector:
        text:

update_child:
  fields:
    child_id:
      required: true
      example: kid_1
      selector:
        text:

    name:
      required: true
      example: Alexander
      selector:
        text:

add_chore:
  fields:
    title:
      required: true
      example: Make the bed
      selector:
        text:

    category:
      required: true
      example: Morning
      selector:
        text:

    points:
      required: true
      example: 2
      selector:
        number:
          min: 1
          max: 100
          step: 1
          mode: box

    icon:
      required: false
      example: mdi:bed
      selector:
        icon:

    sort_order:
      required: false
      example: 10
      selector:
        number:
          min: 0
          step: 1
          mode: box

    child_ids:
      required: false
      example:
        - kid_1
        - kid_2
      selector:
        text:
          multiple: true

add_assignment:
  fields:
    child_id:
      required: true
      example: kid_2
      selector:
        text:

    chore_id:
      required: true
      example: chore_1
      selector:
        text:

assign_chores_to_child:
  fields:
    child_id:
      required: true
      example: kid_2
      selector:
        text:

    chore_ids:
      required: true
      example:
        - chore_1
        - chore_2
      selector:
        text:
          multiple: true

remove_chores_from_child:
  fields:
    child_id:
      required: true
      example: kid_2
      selector:
        text:

    chore_ids:
      required: true
      example:
        - chore_1
        - chore_2
      selector:
        text:
          multiple: true

set_assignment_active:
  fields:
    assignment_id:
      required: true
      example: assignment_1
      selector:
        text:

    active:
      required: true
      example: false
      selector:
        boolean:

update_chore:
  fields:
    chore_id:
      required: true
      example: chore_1
      selector:
        text:

    title:
      required: false
      example: Make the bed properly
      selector:
        text:

    category:
      required: false
      example: Morning
      selector:
        text:

    points:
      required: false
      example: 3
      selector:
        number:
          min: 1
          max: 100
          step: 1
          mode: box

    icon:
      required: false
      example: mdi:bed
      selector:
        icon:

    sort_order:
      required: false
      example: 20
      selector:
        number:
          min: 0
          step: 1
          mode: box

set_child_active:
  fields:
    child_id:
      required: true
      example: kid_1
      selector:
        text:

    active:
      required: true
      example: false
      selector:
        boolean:

set_chore_active:
  fields:
    chore_id:
      required: true
      example: chore_1
      selector:
        text:

    active:
      required: true
      example: false
      selector:
        boolean:


delete_assignment:
  fields:
    assignment_id:
      required: true
      example: assignment_1
      selector:
        text:

delete_child:
  fields:
    child_id:
      required: true
      example: kid_1
      selector:
        text:

delete_chore:
  fields:
    chore_id:
      required: true
      example: chore_1
      selector:
        text:
