# Chores Manager 0.7.0

- Removes the caller-specific `can_adjust` response field.
- Allows authenticated Home Assistant users to adjust weekly points.
- Keeps weekly-total reads and administrator commands separately protected.

No storage migration is required.
